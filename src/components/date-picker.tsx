import React from 'react';
import { Grid } from '@material-ui/core';

const DatePicker: React.FC = () => {
  // The first commit of Material-UI
  return <Grid>datepicker</Grid>;
};

export default DatePicker;
